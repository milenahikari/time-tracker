import { Text } from 'react-native';

import GlobalContainer from '~/src/components/GlobalContainer';

export default function myProductivity() {
  return (
    <GlobalContainer>
      <Text className='font-rubikMedium text-lg color-gray-50'>myProductivity</Text>
    </GlobalContainer>
  );
}